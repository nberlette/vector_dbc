#pragma once

#ifdef _WINDOWS

/* '..' : class '..' needs to hvae dll-interface to be used by clients of class '..' */
#pragma warning (disable: 4251)

/* nonstandard extension used: enum '...' used in qualified name */
#pragma warning (disable: 4482)

#endif
